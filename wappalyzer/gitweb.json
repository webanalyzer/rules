{
    "implies": [
        "Perl",
        "git"
    ],
    "name": "gitweb",
    "website": "http://git-scm.com",
    "matches": [
        {
            "offset": 1,
            "regexp": "<!-- git web interface version ([\\d.]+)?"
        },
        {
            "offset": 1,
            "search": "meta[generator]",
            "regexp": "gitweb(?:/([\\d.]+\\d))?"
        }
    ]
}