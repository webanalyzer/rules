{
    "name": "TornadoServer",
    "website": "http://tornadoweb.org",
    "matches": [
        {
            "offset": 1,
            "search": "headers[server]",
            "regexp": "TornadoServer(?:/([\\d.]+))?"
        }
    ]
}