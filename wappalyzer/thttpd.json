{
    "name": "thttpd",
    "website": "https://acme.com/software/thttpd",
    "matches": [
        {
            "offset": 1,
            "search": "headers[server]",
            "regexp": "\\bthttpd(?:/([\\d.]+))?"
        }
    ]
}