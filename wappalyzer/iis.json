{
    "website": "http://www.iis.net",
    "name": "IIS",
    "implies": "Windows Server",
    "matches": [
        {
            "search": "headers[server]",
            "regexp": "^(?:Microsoft-)?IIS(?:/([\\d.]+))?",
            "offset": 1
        }
    ]
}